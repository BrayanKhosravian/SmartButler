<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:registerable="clr-namespace:SmartButler.Views.Registerable;assembly=SmartButler"
             xmlns:controls="clr-namespace:SmartButler.Controls;assembly=SmartButler"
             xmlns:cells="clr-namespace:SmartButler.Controls"
             x:Class="SmartButler.Views.DrinksPage">

    <NavigationPage.TitleView>
        <controls:ToolBarControl BindingContext="{Binding ToolbarControlViewModel, Mode=TwoWay}"/>
    </NavigationPage.TitleView>


    <ContentPage.Content>
        <StackLayout>
            <ListView ItemsSource="{Binding Drinks}"
                      ItemTapped="Handle_ItemTapped"
                      CachingStrategy="RecycleElement" HasUnevenRows="true">

                <ListView.ItemTemplate>
                    <DataTemplate>
                        <cells:DrinkViewCell/>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <Button Text="AddBottle" Command="{Binding AddBottleCommand}" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>