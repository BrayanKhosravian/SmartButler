<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:views="clr-namespace:SmartButler.Views;assembly=SmartButler"
             xmlns:controls="clr-namespace:SmartButler.Controls;assembly=SmartButler"
             x:Class="SmartButler.Views.BluetoothPage">

    <NavigationPage.TitleView>
        <controls:ToolBarControl BindingContext="{Binding ToolbarControlViewModel, Mode=TwoWay}"/>
    </NavigationPage.TitleView>


    <AbsoluteLayout>
        <ActivityIndicator AbsoluteLayout.LayoutBounds="0.5, 0.5, 0.5, 0.5" AbsoluteLayout.LayoutFlags="All"
            IsRunning="{Binding IsBusy}" 
            IsVisible="{Binding IsBusy}" 
            IsEnabled="{Binding IsBusy}"/>
        <StackLayout>
            <ListView ItemsSource="{Binding BluetoothDevices}"
                      ItemTapped="Handle_ItemTapped"
                      CachingStrategy="RecycleElement">

                <!--Built in Cells-->
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout>
                                <Label Text="{Binding Name}" />
                                <Label Text="{Binding Mac}" />
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>

            </ListView>

            <Button Text="SendCommand" Command="{Binding SendCommand}" />
        </StackLayout>
    </AbsoluteLayout>
</ContentPage>
